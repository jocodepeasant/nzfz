# Tasks

- [x] Task 1: 修复 _show_preview 中 traps 字段拼接逻辑
  - [x] SubTask 1.1: 修改 `script_tab.py` 的 `_show_preview` 方法，将 `", ".join(traps)` 改为兼容 dict 和 str 的逻辑：提取 `trap_name` 或 `trap_id`，再拼接

# Task Dependencies

- 无
