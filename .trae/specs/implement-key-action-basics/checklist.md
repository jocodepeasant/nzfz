- [x] press_key 短按逻辑正确调用 pynput Controller.press + release
- [x] press_key 长按逻辑在 hold_ms > 0 时执行 press → sleep → release
- [x] press_key 特殊键映射正确将 "enter"/"space"/"esc" 等转为 pynput.keyboard.Key 枚举
- [x] press_key 在 pynput 不可用时打印 warning 并抛出 RuntimeError
- [x] click_at 正确调用 pyautogui.click(x, y, button=button)
- [x] click_at 支持 button="left" 和 button="right"
- [x] click_at 在 pyautogui 不可用时打印 warning 并抛出 RuntimeError
- [x] drag 正确执行 moveTo → mouseDown → moveTo → mouseUp 拖拽流程
- [x] drag 的 duration_ms 正确转换为秒传给 pyautogui
- [x] drag 在 pyautogui 不可用时打印 warning 并抛出 RuntimeError
- [x] ensure_map_open 在地图已打开时直接返回 True
- [x] ensure_map_open 在地图未打开时按 O 键并等待后再次检测
- [x] ensure_map_open 最多重试 3 次，全部失败返回 False
- [x] execute_action 对 type="log" 正确打印消息并返回成功结果
- [x] execute_action 对未实现的已知类型返回 not implemented 错误
- [x] execute_action 对未知类型打印 warning 并返回错误
- [x] engine/__init__.py 正确导出所有新增公共函数
- [x] pynput/pyautogui 不可用时模块导入不崩溃，仅在调用时降级
- [x] 所有测试通过（pytest tests/test_action.py）
