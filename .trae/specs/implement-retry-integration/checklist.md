- [ ] ActionContext 数据类包含 capture、rect、rois、slots、regions、runtime、slot_id 字段
- [ ] _build_reset_view_fn 回调正确调用 go_to_origin
- [ ] _build_micro_adjust_fn 回调正确调用 click_slot(micro_adjust=True)
- [ ] _build_micro_adjust_fn slot_id 为 None 时不调用 click_slot
- [ ] 条件满足时直接执行动作，返回 ActionResult(success=True)
- [ ] 条件不满足 + on_condition_failed.policy=skip 时返回 ActionResult(skipped=True)
- [ ] 条件不满足 + on_condition_failed.policy=wait 时等待条件满足
- [ ] 条件不满足 + on_condition_failed.policy=wait 超时时返回 ActionResult(skipped=True)
- [ ] 动作执行失败 + 重试后成功时返回 ActionResult(success=True, attempts=N)
- [ ] 动作执行失败 + 重试耗尽 + on_fail=skip 时返回 ActionResult(skipped=True)
- [ ] 动作执行失败 + 重试耗尽 + on_fail=abort 时抛出 ActionAbortedError
- [ ] reset_view_before_retry=True 时重试前调用 go_to_origin
- [ ] micro_adjust_on_retry=True 且 slot_id 不为 None 时重试前调用 click_slot
- [ ] 无条件配置时跳过条件检查直接执行动作
- [ ] RetryManager 配置解析正确合并动作配置和运行时默认值
- [ ] engine/__init__.py 新增导出 execute_with_conditions 和 ActionContext
- [ ] 单元测试覆盖所有集成场景、边界情况和降级策略
