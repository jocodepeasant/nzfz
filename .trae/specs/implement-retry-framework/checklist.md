- [x] RetryConfig 数据类定义完整，含 max_count / interval_ms / reset_view_before_retry / micro_adjust_on_retry 字段及默认值
- [x] RetryConfig.from_dict 支持从字典和 None 构造
- [x] OnConditionFailedPolicy 枚举包含 WAIT 和 SKIP 两个值
- [x] OnFailPolicy 枚举包含 SKIP 和 ABORT 两个值
- [x] OnConditionFailedConfig 数据类定义完整，含 policy / timeout_ms / then 字段及默认值，支持 from_dict
- [x] OnFailConfig 数据类定义完整，含 policy 字段及默认值，支持 from_dict
- [x] ActionResult 数据类定义完整，含 success / skipped / attempts / data 字段
- [x] ActionAbortedError 异常类定义
- [x] RetryManager.__init__ 接收 runtime_defaults 字典并正确存储
- [x] RetryManager.resolve_retry_config 正确合并动作级配置与运行时默认值
- [x] RetryManager.resolve_on_condition_failed 正确合并动作级配置与运行时默认值
- [x] RetryManager.resolve_on_fail 正确解析 on_fail 配置
- [x] RetryManager.wait_for_condition 支持 policy=wait 轮询和 policy=skip 立即返回
- [x] RetryManager.execute_with_retry 支持重试循环、interval 等待、回调调用、异常捕获
- [x] RetryManager.execute_with_retry 在 on_fail=skip 时返回 ActionResult(success=False, skipped=True)
- [x] RetryManager.execute_with_retry 在 on_fail=abort 时抛出 ActionAbortedError
- [x] engine/__init__.py 正确导出所有新增公开类型
- [x] 单元测试覆盖：数据类构造与默认值、枚举值、配置合并、条件等待、重试执行、回调调用、异常处理、on_fail 策略
